import React from "react";
import { StyleSheet, View } from "react-native";

export default DeletionPopUp = (deleteTodo) => {
  return (
    <View style={styles.componentContainer}>
      <View style={styles.popUpBox}></View>
    </View>
  );
};

const styles = StyleSheet.create({
  componentContainer: {},
  popUpBox: {},
});
